source 'http://rubygems.org'

gem 'activesupport'
gem 'i18n'

group :development do
  gem 'rspec'

  # Autotest
  # --> autotest
  # gem 'rspec-autotest'
  # gem 'autotest-standalone'

  # Guard
  # --> guard init rspec (generates Guardfile)
  # --> bundle exec guard
  gem 'guard-rspec', require: false
end
